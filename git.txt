git init -- 初始化git仓库
git add <file> --添加文件
git commit -m <message> -- 把文件提交到仓库
git status --查看哪些文件被修改了，掌握工作区的状态
git diff --查看修改内容
git log --查看最近3次提交日志，可以加上--pretty=oneline参数，更好看
git reset --hard HEAD^，head是当前版本，HEAD^就是上一个版本